{
  "rules": [
    {
      "allowed": {
        "origins": ["*"],
        "methods": ["GET", "HEAD"],
        "headers": ["*", "Range"]
      },
      "exposeHeaders": ["Content-Range", "Content-Length"],
      "maxAgeSeconds": 86400
    }
  ]
}
