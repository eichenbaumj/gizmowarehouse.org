# Claude Code Usage Analysis — source

No source files in this repo. [The gizmo](../../src/content/claude-code-usage.ts) is a written analysis; the underlying data was Joe's local `~/.claude/projects/*.jsonl` session history, processed in a Claude Code session rather than via a checked-in script.

The exact prompt used to generate the analysis is reproduced in the gizmo's markdown so anyone can run the same pass against their own `~/.claude/` directory.
