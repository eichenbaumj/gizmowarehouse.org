# Introducing Daf Yomi Dot Dev — source

The site itself lives in its own repo (`~/Projects/personal/daf-yomi`, GitHub `eichenbaumj/daf-yomi`,
private) and runs at <https://daf-yomi.dev>. This gizmo is a short post that points there and explains
how the AI note is engineered; the post's text is [`src/content/daf-yomi.ts`](../../src/content/daf-yomi.ts).

Nothing else is here on purpose: no build scripts, no data. The warehouse convention for an artifact
hosted elsewhere is the same one `reducing-violence-whitepaper` uses (a `links[]` entry and a summary).
